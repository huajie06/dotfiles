# Zsh

Repo-backed zsh config.

Use this from your real `~/.zshrc`:

```zsh
source "$HOME/repos/dotfiles/zsh/.zshrc"
```

The prompt uses Tokyo Night colors and shows the current git branch when the
working directory is inside a git repo.
